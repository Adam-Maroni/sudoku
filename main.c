#include "sudoku.h"

int main()
{
    t_pt first_cell;
    char grid[9][9];

    //creation de la grid
    //Completer vide avec 0
    //Trouver premiere cellule a raiter
    if (backtracking(grid,))
        //affihcer tableau
    else
        printf("INSOLVABLE\n");
    return (0);
}